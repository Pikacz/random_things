`pawel_renderer.h` - you can play with `renderToBitmap` in order to display pixels on screen

Other:
* pressing on window border do not block main loop
* simple logging is set up (printf syntax for OutputDebugStringW)